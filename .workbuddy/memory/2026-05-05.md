# 2026-05-05 Log

## v1.6.1 修复 — SMTP 调试模式 + GitHub 推送 + APK 构建（09:25 ~）

### 问题
- 用户反馈：移动端无法注册（收不到验证码），旧用户无法登录
- 服务端 `.env` 未配置 SMTP，验证码邮件发不出去
- 新独立服务端数据库为空，旧用户账号不存在
- GitHub 仓库缺少源码（之前从未推送成功）

### 修复内容

#### 服务器端（server/app_api.php）
- `send_email_code` 端点：检测 `SMTP_HOST` 是否配置
  - 未配置时直接返回 `debug_code`（验证码明文）给客户端
  - 配置但发送失败时也返回 `debug_code`
  - 配置且发送成功时正常返回（无 debug_code）

#### 移动端（App.tsx + src/api.ts）
- `sendEmailCode()` 返回值从 `Promise<void>` 改为 `Promise<{ debug_code?: string }>`
- 注册页面 `sendCode` 函数接收 `debug_code` 并：
  - 自动填入邮箱验证码输入框
  - 显示调试提示框（紫色背景，显示验证码明文）
- 新增 `Notice` type 支持 `'warning'` tone
- 新增 `warningSnack` 样式（tertiary 色背景）
- 新增 `debugCodeBox` / `debugCodeText` / `debugCodeValue` 样式

#### 数据库种子脚本（server/seed.php）
- `php seed.php` 一键创建管理员（admin/admin888）和测试用户（test/test8888）
- 显示当前用户列表
- 生成管理员 Access Token 方便 curl 测试

#### 配置
- 版本号 v1.5.0 → v1.6.1（config.ts + app.json）
- android/gradle.properties 添加 AIF_RELEASE_* 签名参数
- 所有代码已提交并推送到 GitHub

### GitHub
- 仓库：https://github.com/djfuni/AIFChat
- 清理了 .jdk17 大文件（122MB）的 Git 历史
- 成功推送 master 分支

### APK 构建
- 构建中（Gradle assembleRelease）
- 使用 aif-release.keystore 签名
